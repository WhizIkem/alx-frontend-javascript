// interface.ts
// create a type RowID and set it equal to number

export type RowID = number;

// create interface RowElement 
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
